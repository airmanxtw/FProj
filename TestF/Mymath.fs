namespace TestF
module Mymath =
    let add x y = x + y
    let sub x y = x - y
    let mul x y = x * y
    let div x y = x / y
    let pow x y = pown x y
    let sqrt x = sqrt x
    let log x = log x
    let exp x = exp x
    let sin x = sin x
    let cos x = cos x
    let tan x = tan x